management:
  security:
    enabled: false

cgi:
  jms:
    destination.name: CP_OutboundQueue

spring:
  artemis:
    mode: native
    host: "localhost"
    user: "jmsuser"
    password: "jmsuser"
    port: 5445

court-case-service:
  base-url: http://localhost:8090
  disable-authentication: true
  min-backoff-seconds: 1
  max-retries: 1

offender-search:
  base-url: http://localhost:8090
  disable-authentication: true
  ping-path: /ping
  min-backoff-seconds: 1
  max-retries: 1
  use-dob-with-pnc: true

nomis-oauth:
  base-url: http://localhost:8090
  disable-authentication: true
  ping-path: /ping

ou-code-length: 5

# Localstack settings
aws:
  region_name: eu-west-2
  access_key_id: foobar
  secret_access_key: foobar
  sqs:
    crime_portal_gateway_endpoint_url: "http://localhost:4566"
    crime_portal_gateway_queue_name: "crime-portal-gateway-queue"
    crime_portal_gateway_access_key_id: foobar
    crime_portal_gateway_secret_access_key: foobar
    court_case_matcher_endpoint_url: "http://localhost:4566"
    court_case_matcher_queue_name: "court-case-matcher-queue"
    court_case_matcher_access_key_id: foobar
    court_case_matcher_secret_access_key: foobar
  sns:
    court_case_matcher_endpoint_url: "http://localhost:4566"

messaging:
  activemq:
    enabled: false

