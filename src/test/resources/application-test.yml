management:
  security:
    enabled: false

spring:
  artemis:
    mode: native
    host: "localhost"
    user: "jmsuser"
    password: "jmsuser"
    port: 5445

court-case-service:
  base-url: http://localhost:8090
  disable-authentication: true
  min-backoff-seconds: 1
  max-retries: 1

offender-search:
  base-url: http://localhost:8090
  disable-authentication: true
  ping-path: /ping
  min-backoff-seconds: 1
  max-retries: 1
  use-dob-with-pnc: true

nomis-oauth:
  base-url: http://localhost:8090
  disable-authentication: true
  ping-path: /ping

ou-code-length: 5

# Localstack settings
aws:
  region_name: eu-west-2
  access_key_id: foobar
  secret_access_key: foobar
  sqs:
  sns:
    court_case_matcher_endpoint_url: "http://localhost:4566"

aws_sqs_court_case_matcher_endpoint_url: "http://localhost:4566"
aws_sqs_court_case_matcher_queue_name: "court-case-matcher-queue"
aws_sqs_court_case_matcher_dlq_name: "court-case-matcher-dlq"
aws_sqs_court_case_matcher_access_key_id: foobar
aws_sqs_court_case_matcher_secret_access_key: foobar
aws_sqs_court_case_matcher_dlq_endpoint_url: "http://localhost:4566"
aws_sqs_court_case_matcher_dlq_access_key_id: "access-key-id-dlq"
aws_sqs_court_case_matcher_dlq_secret_access_key: "access-key-dlq"
aws_sqs_court_case_matcher_dlq_queue_name: "court-case-matcher-dlq"
